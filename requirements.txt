# Core MCP and BigQuery dependencies
mcp>=1.0.0
google-cloud-bigquery>=3.35.0
google-auth>=2.0.0
anyio>=3.0.0

# HTTP transport dependencies (optional - only needed for HTTP mode)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
sse-starlette>=1.6.5
websockets>=12.0
pydantic>=2.0.0
PyYAML>=6.0.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
aiohttp>=3.8.0
requests>=2.28.0
